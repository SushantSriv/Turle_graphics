import turtle
for steps in range(10000000000000000000):
    for steps in range(4):
        turtle.color('green')
        turtle.forward(100)
        turtle.right(90)
    for steps in range(4):
        turtle.color('red')
        turtle.forward(100)
        turtle.left(90)
    for steps in range(4):
        turtle.color('blue')
        turtle.forward(100)
        turtle.right(90)    
    for steps in range(4):
        turtle.color('yellow')
        turtle.forward(100)
        turtle.left(90)
    for steps in range(4):
        turtle.color('white')
        turtle.forward(100)
        turtle.right(90)
    for steps in range(4):
        turtle.color('pink')
        turtle.forward(100)
        turtle.left(90)   
